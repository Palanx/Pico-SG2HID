# Phase index — pico-sg2hid

<!-- The single source of truth for execution state. Machine-parsed (grep/awk)
     and human-read after compaction — keep the table format EXACT.

     Status vocabulary (only these six, lowercase):
       pending      indexed, not yet expanded
       expanded     spec.md written, not started
       in-progress  implementation started (also: failed validation, being fixed)
       blocked: <reason>   needs an operator decision — reason is mandatory
       superseded by <ids> this cut turned out wrong; the ids that replace it
       done         validation passed; only /validate-phase writes this

     Rules:
       - `depends` lists phase ids, comma-separated, or `-`. These are edges,
         not an ordering: rows with no path between them may run in parallel.
       - Acceptance here is the coarse, one-line form; the executable form
         lives in the phase's spec.md once expanded.
       - Rows are append-only per feature section; never renumber ids. -->

## Feature: driverless SG-to-HID firmware  (/bootstrap-project 2026-08-31)

Turns a wired PlayStation 2 Guitar Hero SG into a USB HID gamepad on macOS and Debian,
with the whammy bar as a real analog axis, using a Raspberry Pi Pico. Deliberately out
of scope: the wireless Les Paul, any other PS2 device, rumble, Windows, the Pico 2, and
any host-side software — see `docs/product/requirements.md` §Non-goals.

The ordering is a safety property, not a preference (ADR-0004, ADR-0006): pure logic
first, then one Pico in loopback, then Pico against Pico, then the real guitar observed
read-only, and only then the real guitar in full. Phases `01` and `02` have no path
between them and may run in parallel.

| id | goal | depends | acceptance (coarse) | status |
|----|------|---------|---------------------|--------|
| 00-scaffold | Repo-shape checks and the rule↔test traceability meta-test | - | `make test` runs green from a clean clone on macOS with Homebrew LLVM, with only clang++ and python3; every check is proven live by mutations generated from its own source — no alternative of any check pattern can be deleted, and no check neutered, with the suite still green — and the fourteen rules marked `planned: 00-scaffold` move to `test:` | done |
| 01-ps2-codec | Pure PS2 frame codec, controller-id table, negotiation state machine, button/axis map, HID report builder — all in `src/core/`, no hardware | 00-scaffold | Host tests decode every literal vector in `tests/vectors/` correctly, including a missing-`ACK` abort and an unknown controller id; R-PROTO-02..04 move to `test:` | done |
| 02-wiring | GPIO assignment, the `src/core/pins.h` table, the wiring document, and a multimeter checklist that teaches the operator to use one | 00-scaffold | Pin-table tests enforce R-SAFETY-01..03; the operator completes the continuity and voltage checklist with the guitar unplugged and records the SG's current draw, answering the two OPEN items in requirements.md | pending |
| 03-pio-bus | PIO bus master: clock generation, LSB-first shift, `ACK` wait with timeout, `ATT` framing | 01-ps2-codec, 02-wiring, 13-scaffold-check-gaps | One Pico in loopback (`CMD` wired to `DATA`) round-trips known byte sequences at the target clock rate; every error and timeout path releases `ATT` (R-SAFETY-07 to `test:`) | pending |
| 04-trace-mode | USB CDC bus trace from the firmware plus the host decoder in `tools/` | 03-pio-bus | A loopback session produces a trace the decoder renders as readable frames with per-byte `ACK` timings, and the operator can read one and say what happened on the bus | pending |
| 05-emulator | Second-Pico firmware in `src/emu/` that behaves as an SG: id, report bytes, `ACK` timing, digital or analog on command, plus injectable faults | 01-ps2-codec, 02-wiring | The emulator answers a manually driven frame correctly and can be told to drop an `ACK`, answer late, or report a wrong id; R-SAFETY-06 moves to `test:` | pending |
| 06-hil-digital | Hardware-in-the-loop: the master polls the emulator in digital mode | 03-pio-bus, 05-emulator | An automated two-Pico run polls for a sustained period with zero desyncs, and each injected fault produces the specified recovery instead of a hang | pending |
| 07-analog-mode | Config-mode sequence, analog-mode verification, whammy axis, against the emulator | 06-hil-digital | The master negotiates analog mode, verifies the analog header before reading the axis, and reads the emulator's swept whammy value end to end | pending |
| 08-usb-hid | TinyUSB HID gamepad descriptor and report transport, fed by emulator data | 07-analog-mode | macOS and Debian both enumerate the device with no driver and show every fret, both strums, and a continuous whammy axis in a standard gamepad tester | pending |
| 09-guitar-observe | First contact with the real SG, read-only through trace mode; reconcile the captured bytes with the codec | 04-trace-mode, 08-usb-hid | The operator captures real traces following a written procedure; every discrepancy against the codec becomes a new literal vector and a codec fix, or a recorded finding in `docs/constraints.md` | pending |
| 10-guitar-full | Full pipeline on the real guitar: negotiation, polling, hotplug recovery, latency | 09-guitar-observe | The guitar plays through a gamepad tester on both hosts across repeated unplug/replug cycles with measured end-to-end latency inside one USB poll interval | pending |
| 11-release | Flashable UF2, build and flash instructions, host-side setup notes for both OSes | 10-guitar-full | A clean clone builds the UF2 following only the README, and a second machine flashes it and plays without further steps | pending |
| 12-scaffold-scope-clauses | fix 00-scaffold: R-STYLE-02, R-CLEAN-02, R-PROC-02 and R-ERR-03 assert more than their checks measure and their texts do not say so — each rule's own text gains a scope clause naming what its check does not see (`docs/phases/01-ps2-codec/notes.md` §For later phases — added by the round-15 audit) | - | Each of the four rule lines in `docs/constraints.md` states its check's scope and what it gives up — `tidy_sources()`'s `src/core/*` + `tests/*.cpp` reach for R-STYLE-02 and R-CLEAN-02, existence-not-content for R-PROC-02, comment stripping for R-ERR-03 — where today none of the four does; `make test` stays green | done |
| 13-scaffold-check-gaps | fix 00-scaffold: five gaps `12-scaffold-scope-clauses` recorded in rule text but left open — `tidy_sources()` never reaches `src/hal`, `src/usb`, `src/app` or `src/emu`, so R-STYLE-02, R-CLEAN-02 and R-CLEAN-04 go silent on the first firmware file there; `hits( )` misses code after a `//` inside a string literal (R-ERR-03); `reject( )`/`accept( )` in `tests/test_repo_shape.sh` take four parameters against R-CLEAN-02; R-PROC-02's check accepts any non-empty `verify.md`; `src_files( )` in `tests/test_repo_shape.sh` lists only `*.cpp` and `*.h`, so every rule it backs skips `.hpp`, `.cc` and `.inl` under `src/` (`docs/phases/12-scaffold-scope-clauses/notes.md` §For later phases) | - | Each of these fails today and passes after: a naming violation planted under `src/hal/` is reported by `make lint`; `"http://x"; throw E;` under `src/` is reported by R-ERR-03; no shell function in `tests/` takes more than three parameters; a one-line `verify.md` on a `done` phase fails R-PROC-02; a `throw` planted in a `.hpp` under `src/` is reported by R-ERR-03 — and each closed gap's scope clause in `docs/constraints.md` is removed or narrowed in the same edit as its check, with `make test` and `make lint` green | done |
| 14-scaffold-nongrep-clauses | fix 00-scaffold: R-ARCH-02, R-SEC-01, R-TOOL-01, R-TOOL-02 and R-PROC-01 are bound to checks that are not greps (`tests/test_boundaries.sh`, `tests/test_secrets.sh`, `tests/test_tool_versions.sh`, `tests/test_rule_traceability.py`) and their texts state no scope, which asserts full coverage — each rule's own text gains a measured scope clause naming what its check does not see (`docs/phases/01-ps2-codec/spec.md` §A check that is not a grep owes a clause too) | - | Each of the five rule lines in `docs/constraints.md` carries a `**Scope, recorded` clause measured against its check, where today none does; bindings and checks unchanged; `make test` stays green | done |
| 15-scaffold-file-lists | fix 00-scaffold: `core_files( )` and `core_headers( )` in `tests/test_repo_shape.sh`, and `sources()` and `tidy_sources()` in `tests/test_style.sh`, stop at `.cpp`/`.h` while `src_files( )` reads `.cpp`, `.h`, `.hpp`, `.cc`, `.inl` — so R-ARCH-01, R-CLEAN-09, R-ERR-01, R-ERR-02, R-STYLE-01, R-STYLE-02, R-CLEAN-02 and R-CLEAN-04 skip `.hpp`, `.cc` and `.inl`; plus the stale "Today no phase is done" header comment in `tests/test_phase_docs.sh` (`docs/phases/13-scaffold-check-gaps/notes.md` §For later phases; `.claude/rules/tech-debt.md` "File lists that stop at `.cpp` and `.h`", option (b) chosen 2026-09-23) | - | Each of these fails today and passes after: a `virtual` planted in a `.hpp` under `src/core/` is reported by R-CLEAN-09; a misformatted `.hpp` fails R-STYLE-01; a naming violation in a `.hpp` under `src/` is reported by `make lint` — the extension clauses `13-scaffold-check-gaps` wrote into R-STYLE-02, R-CLEAN-02 and R-CLEAN-04 are removed or narrowed in the same edit as their list, the liveness harness catches every new alternation, and `make test` and `make lint` stay green | done |
| 16-scaffold-spelling-clauses | fix 00-scaffold: R-ARCH-01, R-ARCH-03, R-CLEAN-03, R-CLEAN-05, R-CLEAN-09, R-ERR-03 and R-ERR-04 are bound to `find_*` greps with spellings they do not match, and their texts state no spelling clause (R-ERR-03's clause covers the scanner only), which asserts full coverage; R-CLEAN-03 and R-CLEAN-05 also owe a file-scope clause for `src_files( )` — each rule's own text gains a measured scope clause, shaped like `12-scaffold-scope-clauses` (`.claude/rules/tech-debt.md` "Unwritten spelling and file-scope clauses for seven grep-bound rules"; depends on 15 because the clauses describe the file lists 15 changes) | 15-scaffold-file-lists | Each of the seven rule lines in `docs/constraints.md` carries a `**Scope, recorded` clause naming the spellings its finder was measured to miss — R-ERR-03's extended, with `try` and `{` on separate lines among them — where today six carry none; bindings and checks unchanged; `make test` stays green | done |
| 17-scaffold-sweep-spaces | fix 00-scaffold: `sweep( )` in `tests/test_boundaries.sh` iterates the unquoted output of `find`, so a file under `src/` whose name contains a space is split into two nonexistent paths and never reaches `.claude/hooks/boundary-check.sh` (R-ARCH-02; measured in `docs/phases/14-scaffold-nongrep-clauses/notes.md` §Outcome) — iterate NUL-separated paths, and drop that sentence from R-ARCH-02's scope clause in the same edit. The hook's own misses (`#include <hal/…>`, `# include`) are package-owned and go through `/belay-feedback`, not this row | - | A layer violation in `src/core/a b.cpp` fails `tests/test_boundaries.sh` (today it passes), with a rejection case proving it; R-ARCH-02's clause no longer names the space-in-filename miss; `make test` green | superseded by 20-scaffold-check-harness-fixes |
| 18-scaffold-traceability-gaps | fix 00-scaffold: `check( )` in `tests/test_rule_traceability.py` accepts a `RULE R-X-011` marker as `R-X-01` (neither `MARKER` nor the substring test is bounded after the id), never reads a rule written as `* **R-…**` or indented, and crashes with `IsADirectoryError` on a `test:` path that is a directory (R-PROC-01; measured in `docs/phases/14-scaffold-nongrep-clauses/notes.md` §Outcome) — close all three and remove them from R-PROC-01's scope clause in the same edit | - | Each of the three forms is a rejection case that `check( )` reports as a problem instead of passing or crashing; R-PROC-01's clause no longer names them; `make test` green | superseded by 20-scaffold-check-harness-fixes |
| 19-scaffold-ver-num | fix 00-scaffold: `ver_num( )` in `tests/test_tool_versions.sh` reads the first number in a banner's first line, so `x86_64-apple clang-format version 14.0.6` is read as 86 and `clang-format 99 version 14.0.6` as 99, both clearing a floor of 23 (R-TOOL-01; measured in `docs/phases/14-scaffold-nongrep-clauses/notes.md` §Outcome) — read the number after the word `version` when the banner has one, and drop those forms from R-TOOL-01's scope clause in the same edit | - | A stub banner `x86_64-apple clang-format version 14.0.6` fails the clang-format floor (today it passes), with a rejection case proving it; R-TOOL-01's clause no longer names the first-number miss; `make test` green | superseded by 20-scaffold-check-harness-fixes |
| 20-scaffold-check-harness-fixes | fix 00-scaffold: replaces 17-scaffold-sweep-spaces, 18-scaffold-traceability-gaps and 19-scaffold-ver-num as one phase (operator's cut, 2026-09-24). Five check gaps measured in `docs/phases/14-scaffold-nongrep-clauses/notes.md` §Outcome: (1) `sweep( )` in `tests/test_boundaries.sh` iterates the unquoted output of `find`, so a file under `src/` whose name contains a space never reaches the boundary hook (R-ARCH-02); in `check( )` of `tests/test_rule_traceability.py` (R-PROC-01), (2) a `RULE R-X-011` marker satisfies `R-X-01` because neither `MARKER` nor the substring test is bounded after the id, (3) a rule written as `* **R-…**` or indented is never read, and (4) a `test:` path that is a directory crashes with `IsADirectoryError`; (5) `ver_num( )` in `tests/test_tool_versions.sh` reads the first number of the banner, so `x86_64-apple clang-format version 14.0.6` is read as 86 (R-TOOL-01) — close all five and remove each from its rule's scope clause in the same edit. The boundary hook's own misses are package-owned and go through `/belay-feedback`, not this row | - | Each of the five forms is a rejection case its check now reports (today each passes or crashes); the R-ARCH-02, R-PROC-01 and R-TOOL-01 scope clauses no longer name them; `make test` green | done |
| 21-scaffold-filename-splitting | fix 00-scaffold: file lists are word-split, so a file whose name contains a space is never read (measured in `docs/phases/20-scaffold-check-harness-fixes/notes.md` §For later phases). In `tests/test_repo_shape.sh` every finder passes `$( src_files … )`, `$( core_files … )` or `$( core_headers … )` unquoted, so `hits( )`/`raw_hits( )` skip both halves with `[ -f ]` — R-ARCH-01, R-ARCH-03, R-ERR-01..04, R-CLEAN-03, R-CLEAN-05, R-CLEAN-09, R-PROTO-05. In `tests/test_style.sh`, R-STYLE-01 pipes `sources()` to `xargs clang-format` and fails with `No such file or directory` instead of a formatting diagnosis, and the clang-tidy loop `for f in $files` splits the same way (R-STYLE-02, R-CLEAN-02, R-CLEAN-04; found by reading, not yet measured). Pass every list without splitting and add one rejection case per list and one per `test_style.sh` path. Out of scope: newline in a file name (recorded in R-ARCH-02; POSIX `sh` has no NUL read) and every other form the rules' scope clauses name | - | Each of these passes or misdiagnoses today and is reported correctly after: a `throw` in `src/core/a b.cpp` fails R-ERR-03; a `virtual` in `src/core/a b.hpp` fails R-CLEAN-09 (core_files); a forbidden return type in a spaced `src/core/` header fails R-ERR-02 (core_headers); a misformatted `src/core/a b.cpp` makes R-STYLE-01 print a formatting diff, not `No such file`; a naming violation in `src/core/a b.cpp` is reported by R-STYLE-02; no affected rule's clause claims a space in a file name is seen when it is not; `make test` and `make lint` green | done |
